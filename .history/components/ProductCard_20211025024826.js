
const ProductCard = ({ product }) => {

   const { handle, title,  } = product.node;
   const { image } = product.node.image.fluid;
   return (
      <div>
         
      </div>
   )
}

export default ProductCard
