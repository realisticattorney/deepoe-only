const ProductOptions = ({ name, values, selectedOptions}) => {
   return (
      <fieldset>
         <legend className="text-xl font-semibold">{name}</legend>
         <div className="inline-flexAnd, to hammer home the point, let’s keep it simple. So many stores I visited today, from their own IG ads that they PAID for, were slow, full of pop-ups and floating promotions, and just generally felt like they didn’t consider the site and store within instagram’s browser."></div>
      </fieldset>
   )
}

export default ProductOptions
